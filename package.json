{
  "name": "@nervill/metadelta",
  "version": "0.11.10",
  "description": "Salesforce CLI plugin to find recent metadata changes in an org",
  "type": "module",
  "main": "lib/index.js",
  "exports": "./lib/index.js",
  "files": [
    "lib/",
    "messages/",
    "oclif.manifest.json",
    "README.md"
  ],
  "scripts": {
    "clean": "node -e \"import {rmSync} from 'node:fs';['lib','oclif.manifest.json'].forEach(p=>rmSync(p,{recursive:true,force:true}))\"",
    "compile": "npm run clean && tsc && npm run manifest",
    "test": "node --test",
    "manifest": "node scripts/generate-manifest.mjs",
    "pack": "npm run compile && npm pack",
    "pack:tarball": "npm run compile && mkdir -p dist && npm pack --pack-destination dist && node scripts/create-tarball-alias.mjs"
  },
  "keywords": [
    "salesforce",
    "sf",
    "plugin",
    "metadata"
  ],
  "author": "Nerio Villalobos",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NerioVillalobos/plugin-metadelta.git"
  },
  "bugs": {
    "url": "https://github.com/NerioVillalobos/plugin-metadelta/issues"
  },
  "homepage": "https://github.com/NerioVillalobos/plugin-metadelta",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@oclif/core": "^4.8.0",
    "fast-xml-parser": "^5.3.2"
  },
  "allowScripts": {
    "@oclif/core": false,
    "minimatch": false
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "oclif": {
    "bin": "sf",
    "commands": "./lib/commands",
    "dirname": "metadelta",
    "pluginType": "sf",
    "topicSeparator": " "
  }
}
